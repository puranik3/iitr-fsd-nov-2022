const message = 'Good morning', name = 'John';

// const greeting = message + ' ' + name + '!';
// works ONLY with backtick quotes (``)
const greeting = `${message} ${name}!`;
console.log( greeting );